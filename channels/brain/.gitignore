state.json
.state.tmp.*
wake.log
cron.log
raw/
# Lessons-learned database is runtime data (operator-specific rules,
# mode 600). `lessons.sh init` re-creates it from the seed markdown on
# any fresh checkout.
lessons.db
lessons.db-wal
lessons.db-shm
logo_sampler.log
tg_confirm_offset.txt
confirmation_poller.log
confirmation_poller.log.1
logo_sampler.log.1
